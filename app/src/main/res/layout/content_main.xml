<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.jordy.rsrrevalidatieservice.MainActivity"
    tools:showIn="@layout/activity_main">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/main_bg"
        android:contentDescription="@string/background_image"/>

    <Button
        android:id="@+id/btnPechhulp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/btn_margin_end"
        android:layout_marginLeft="@dimen/btn_margin_start"
        android:layout_marginBottom="@dimen/btn_margin_bottom"
        android:background="@drawable/btn_bg"
        android:drawableLeft="@drawable/main_btn_warning"
        android:text="@string/btnPechhulp"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="@android:color/white"
        android:paddingEnd="@dimen/btn_padding_end"
        android:paddingStart="@dimen/btn_padding_start"
        android:paddingRight="@dimen/btn_padding_end"
        android:paddingLeft="@dimen/btn_padding_start"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>
